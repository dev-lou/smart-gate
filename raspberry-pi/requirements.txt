# Smart School Gate System - Raspberry Pi Dependencies
# =====================================================
# Install with: pip install -r requirements.txt
#
# Note: Some packages are only needed on real Raspberry Pi hardware.
# Packages marked [OPTIONAL] can be skipped for simulation mode.

# Core
opencv-python>=4.8.0
numpy>=1.24.0,<2.0

# Face Recognition
# dlib is required for face_recognition. On Windows, install the prebuilt wheel:
#   pip install https://github.com/z-mahmud22/Dlib_Windows_Python3.x/raw/main/dlib-19.22.99-cp310-cp310-win_amd64.whl
# Or use the face_env venv which already has it installed.
dlib>=19.22.0
face-recognition>=1.3.0
# MediaPipe (alternative face detector) [OPTIONAL]
mediapipe>=0.10.0

# Uniform Detection - YOLO11 [OPTIONAL]
ultralytics>=8.3.0

# Text-to-Speech
pyttsx3>=2.90

# HTTP Client (for cloud sync)
requests>=2.28.0

# Fingerprint Sensor [OPTIONAL - only on Pi with R307]
# pyfingerprint>=1.5

# RFID Reader [OPTIONAL - only on Pi with MFRC522]
# mfrc522>=0.0.7

# GPIO Control [OPTIONAL - only on Raspberry Pi]
# RPi.GPIO>=0.7.1
# gpiozero>=2.0
